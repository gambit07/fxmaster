/**
 * FXMaster: Hooks - Compatibility Redirect
 *
 * Re-exports from the split `hooks/` modules so that existing import paths (`from "./hooks.js"`) continue to work.
 */

export { registerHooks } from "./hooks/index.js";
